PORT=4000
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/cinetrack
JWT_SECRET=changeme
TMDB_API_KEY=changeme

# Web push (optional). Without these, notifications are disabled and the rest
# of the app is unaffected. Generate a pair with:
#   npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:you@example.com
